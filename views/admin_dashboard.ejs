<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <nav class="navbar navbar-dark bg-transparent mb-4 pt-3">
        <div class="container">
            <span class="navbar-brand h1 mb-0"><i class="fas fa-vote-yea"></i> Online Voting System</span>
            <div class="d-flex gap-2">
                <a href="/admin/users" class="btn btn-info text-white btn-sm"><i class="fas fa-users"></i> Manage Users</a>
                <a href="/" class="btn btn-outline-light btn-sm"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10"> <div class="card main-card">
                    
                    <div class="card-header bg-white border-0 pt-4 pb-2">
                        <div class="d-flex justify-content-between align-items-center flex-wrap">
                            <div>
                                <h4 class="mb-1">Admin Dashboard</h4>
                                <p class="text-muted mb-0">Welcome, <%= user.fullName %></p>
                            </div>
                            <a href="/create-election" class="btn btn-success">
                                <i class="fas fa-plus"></i> New Election
                            </a>
                        </div>
                        
                        <form action="/dashboard" method="GET" class="mt-3">
                            <div class="input-group">
                                <input type="text" name="search" class="form-control" placeholder="Search elections by title..." value="<%= locals.searchQuery || '' %>">
                                <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Search</button>
                                <% if(locals.searchQuery) { %>
                                    <a href="/dashboard" class="btn btn-outline-secondary">Clear</a>
                                <% } %>
                            </div>
                        </form>
                    </div>

                    <div class="card-body p-0">
                        <% if(elections.length === 0) { %>
                            <div class="p-5 text-center text-muted">
                                <i class="fas fa-inbox fa-3x mb-3"></i>
                                <p>No elections found.</p>
                            </div>
                        <% } else { %>
                            <div class="list-group list-group-flush">
                                <% elections.forEach(function(election) { %>
                                    <div class="list-group-item p-4">
                                        <div class="row align-items-center">
                                            <div class="col-md-6 mb-3 mb-md-0">
                                                <h5 class="mb-1 fw-bold text-primary"><%= election.title %></h5>
                                                <div class="mb-2">
                                                    <% if(election.status === 'active') { %>
                                                        <span class="badge bg-success">Active</span>
                                                    <% } else if(election.status === 'closed') { %>
                                                        <span class="badge bg-secondary">Closed</span>
                                                    <% } else { %>
                                                        <span class="badge bg-warning text-dark">Draft</span>
                                                    <% } %>
                                                    <span class="text-muted small ms-2">
                                                        <i class="fas fa-poll"></i> <%= election.candidates.reduce((acc, c) => acc + c.voteCount, 0) %> Votes
                                                    </span>
                                                </div>
                                                <small class="text-muted">
                                                    End: <%= new Date(election.endDate).toLocaleDateString() %>
                                                </small>
                                            </div>

                                            <div class="col-md-6 text-md-end">
                                                <div class="btn-group" role="group">
                                                    <a href="/results/<%= election._id %>" class="btn btn-outline-primary btn-sm">
                                                        <i class="fas fa-chart-pie"></i> Results
                                                    </a>
                                                    <a href="/export-csv/<%= election._id %>" class="btn btn-outline-success btn-sm">
                                                        <i class="fas fa-file-csv"></i> CSV
                                                    </a>
                                                    <form action="/delete-election/<%= election._id %>" method="POST" onsubmit="return confirm('Delete this election? Cannot be undone.');" style="display:inline;">
                                                        <button type="submit" class="btn btn-outline-danger btn-sm" style="border-top-left-radius: 0; border-bottom-left-radius: 0;">
                                                            <i class="fas fa-trash"></i>
                                                        </button>
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
